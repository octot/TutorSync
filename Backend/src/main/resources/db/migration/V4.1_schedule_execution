CREATE TABLE schedule_execution (
    id UUID PRIMARY KEY,
    schedule_id UUID REFERENCES schedule(id),
    recipient_type VARCHAR(20), -- TUTOR / PARENT
    status VARCHAR(20),         -- SCHEDULED
    retry_count INT DEFAULT 0,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);